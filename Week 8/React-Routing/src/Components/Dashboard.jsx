export default function Dashboard(){
    return(
        <div>
            Dashboard Page
        </div>
    )
}

//when we use default for exporting we import in the following way "import dashboard" and not "import {dashboard}"