export default function Landing(){
    return(
        <div>
            Landing Page
        </div>

    )
}

//when we use default for exporting we import in the following way "import landing" and not "import {landing}"